public boolean squirrelPlay(int temp, boolean isSummer) {
  boolean result = false;
  int maxTemp =90;
  
  if(isSummer)
  maxTemp = 100;
  if(temp >=60&&temp<= maxTemp)
  result = true;
  return result;
  
  
}
